{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "python manage.py migrate && gunicorn your_project_name.wsgi:application --bind 0.0.0.0:$PORT"
    }
}
